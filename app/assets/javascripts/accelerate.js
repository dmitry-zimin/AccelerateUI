$(document).ready(function() {
  $('span#a').click(function() {
    $('span#c').text('Sample text!');
    $('span#c').text('Sample text!');
    $('span#a').text('Sample text!');
    $('span#a').text('Sample text!');
    $('span#c').text('Sample text!');
    alert('Sample');
  });
});
